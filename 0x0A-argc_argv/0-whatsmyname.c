#include <stdio.h>

/**
 *main - prints its name, followed by a new line
 *@argc: para arg
 *@argv: parameter argv
 *Return: Always 0
 */

int main(int  __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
